diff --git a/packages/credentials/credentials/src/types.ts b/packages/credentials/credentials/src/types.ts
--- a/packages/credentials/credentials/src/types.ts
+++ b/packages/credentials/credentials/src/types.ts
@@ -59,6 +59,18 @@
 /** One durable credential record, tagged by what the seam may do with it. */
 export type CredentialRecord = ApiKeyRecord | GrantRecord
 
+/**
+ * Provider-authored diagnostic safe for configuration UIs and the Remote wire.
+ * Neither field may contain secrets, credential payloads, or raw provider
+ * errors. Providers must map failures to safe codes and messages themselves.
+ */
+export interface CredentialDiagnostic {
+  /** Stable provider-defined diagnostic code. */
+  code: string
+  /** Safe user-facing explanation, not an exception's raw message. */
+  message: string
+}
+
 /**
  * Source and writability facts for one reference, safe for configuration UIs —
  * never the value. The view has no slot a value could ride in, which is what
@@ -71,6 +83,12 @@
   source?: string
   /** Whether the active provider can write this reference. */
   writable: boolean
+  /** Safe display name of the credential manager, including while unconfigured. */
+  owner?: string
+  /** Known expiry as milliseconds since the Unix epoch; does not trigger refresh. */
+  expiresAt?: number
+  /** Safe explanation of unavailable or unusable credentials. */
+  diagnostic?: CredentialDiagnostic
 }
 
 declare module '@deepseek-ai/cordis' {
diff --git a/packages/credentials/credentials/src/index.ts b/packages/credentials/credentials/src/index.ts
--- a/packages/credentials/credentials/src/index.ts
+++ b/packages/credentials/credentials/src/index.ts
@@ -13,7 +13,8 @@
-import type { CredentialInfo, CredentialKey, CredentialRecord, CredentialRef } from './types.ts'
+import type { CredentialDiagnostic, CredentialInfo, CredentialKey, CredentialRecord, CredentialRef } from './types.ts'
 
 export type {
-  ApiKeyRecord, CredentialInfo, CredentialKey, CredentialRecord, CredentialRef, GrantRecord,
+  ApiKeyRecord, CredentialDiagnostic, CredentialInfo, CredentialKey, CredentialRecord, CredentialRef,
+  GrantRecord,
 } from './types.ts'
 
 const REF_PATTERN = /^[A-Za-z_][A-Za-z0-9_]*$/
@@ -135,6 +136,12 @@
   kind?: CredentialRecord['kind']
   /** Whether {@link CredentialProvider.modifyRecord} would currently succeed. */
   writable: boolean
+  /** Safe display name of the credential manager, including while unconfigured. */
+  owner?: string
+  /** Known expiry as milliseconds since the Unix epoch; does not trigger refresh. */
+  expiresAt?: number
+  /** Safe explanation of unavailable or unusable credentials. */
+  diagnostic?: CredentialDiagnostic
 }
 
 /** One stored record's address and tag, for enumeration — never its value. */
@@ -186,7 +193,7 @@
    * Describe one reference for configuration surfaces without exposing the
    * value.
    * @param ref - the reference to describe.
-   * @returns configured state, supplying source, and writability.
+   * @returns configured state, source, writability, and optional safe ownership metadata.
    */
   abstract describe(ref: CredentialRef): Promise<CredentialInfo>
 
@@ -218,8 +225,10 @@
 
   /**
    * Describe one record for configuration surfaces without exposing its value.
+   * Writability is independent of presence: a missing externally managed record
+   * can be read-only. Diagnostics must satisfy {@link CredentialDiagnostic}.
    * @param key - the record to describe.
-   * @returns presence, discriminant, and writability.
+   * @returns presence, discriminant, writability, and optional safe ownership metadata.
    */
   abstract describeRecord(key: CredentialKey): Promise<CredentialRecordInfo>
 
diff --git a/packages/credentials/authorization/src/index.ts b/packages/credentials/authorization/src/index.ts
--- a/packages/credentials/authorization/src/index.ts
+++ b/packages/credentials/authorization/src/index.ts
@@ -262,6 +262,9 @@
    * joined: the two would be prompting different humans through the same flow,
    * and the second would answer questions the first was asked.
    *
+   * After claiming the slot, checks credential writability before running the
+   * flow (and thus before its prompts or network activity), even for missing
+   * records. Withdrawal during this metadata lookup prevents the flow running.
    * @param request - the key, the method, the surface, and the cancel signal.
    * @returns `authorized` once the flow's record is committed during this
    *   attempt and observed, or `cancelled` when the human declined or the
@@ -268,7 +271,8 @@
    *   caller withdrew.
    * @throws {AuthorizationError} code `NO_FLOW` when nothing claims the key,
    *   `UNKNOWN_METHOD` when the named method is not one the flow offers,
-   *   `ALREADY_IN_FLIGHT` when an attempt is already running for the key, or
+   *   `ALREADY_IN_FLIGHT` when an attempt is already running for the key,
+   *   `CREDENTIAL_READ_ONLY` when the credential provider refuses writes, or
    *   `NOT_COMMITTED` when the flow resolved without committing a record
    *   during the attempt.
    */
@@ -299,6 +303,17 @@
     this.running.set(key, { controller })
     let settlement: AuthorizationSettlement = 'failed'
     try {
+      const info = await this.ctx.credentials.describeRecord(key)
+      if (controller.signal.aborted) {
+        settlement = 'cancelled'
+        return { status: 'cancelled' }
+      }
+      if (!info.writable) {
+        const owner = info.owner ? ` (managed by ${info.owner})` : ''
+        throw new AuthorizationError(
+          `credential record "${key}" is read-only${owner}`,
+          'CREDENTIAL_READ_ONLY')
+      }
       const outcome = await this.attempt(flow, method, controller.signal, request.interaction)
       settlement = outcome.status
       return outcome
@@ -367,8 +382,8 @@
     // run is left to finish on its own; nothing waits on it, and a record it
     // still manages to commit is a record the human did authorize.
     const withdrawn = new Promise<'withdrawn'>((resolve) => {
-      // `begin()` returns before claiming the key when its caller has already
-      // withdrawn, so this signal cannot already be aborted here.
+      // `begin()` checks withdrawal again after its asynchronous preflight,
+      // so this signal cannot already be aborted here.
       signal.addEventListener('abort', () => { resolve('withdrawn') }, { once: true })
     })
     // What the seam itself witnessed during the run, held as properties
