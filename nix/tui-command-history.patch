--- a/src/index.ts
+++ b/src/index.ts
@@ -33 +33 @@
-import type { CommandResult } from '@deepseek-ai/dsh-commands'
+import { parseCommand, type CommandResult } from '@deepseek-ai/dsh-commands'
@@ -1560,7 +1560,10 @@
       return
     }
     if (value.startsWith('/')) {
-      editor.addToHistory(text)
+      const parsed = parseCommand(value)
+      if (parsed === undefined || ctx.commands.find(agent, parsed.name)?.recordInput !== false) {
+        editor.addToHistory(text)
+      }
       editor.setText('')
       runCommand(value)
       return
